{# src/Bdx/TutoratBundle/Resources/views/Tutor/show.html.twig #}


{% extends 'BdxTutoratBundle::layout.html.twig' %}


{% block content %}
<h1>Tuteur</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Nom</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ entity.email }}</td>
        </tr>
        <tr>
            <th>Utilisateur associé</th>
            <td>{{ entity.user }}</td>
        </tr>
        <tr>
            <th>Informations</th>
            <td>{{ entity.information }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('tutor') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('tutor_edit', { 'id': entity.id }) }}">
            Modifier la fiche
        </a>
    </li>
    <li>
		<a href="{{ path('tutor_delete', { 'id': entity.id }) }}">
			Supprimer la fiche
		</a>
    </li>
</ul>
{% endblock %}
