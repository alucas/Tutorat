{# src/Bdx/TutoratBundle/Resources/views/RDV/edit.html.twig #}


{% extends 'BdxTutoratBundle::layout.html.twig' %}


{% block content %}
<h1>Modification d'un RDV</h1>

<form action="{{ path('rdv_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Modifier</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('rdv') }}">
            Retour à la liste
        </a>
    </li>
    <li>
		<a href="{{ path('rdv_delete', { 'id': entity.id }) }}">
			Supprimer le RDV
		</a>
    </li>
</ul>
{% endblock %}
